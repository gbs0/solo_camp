<div class="ml-5 p-3 mt-3">
    <div class="d-flex p-2 mb-3 align-items-center">
        
        <div class="row ml-3">
            <h3 style="margin: 0;"><%= @property.name %></h3>
        </div>
    </div>
    <div class='d-flex flex-column col-lg-3' >
        <%= render partial: 'weather', locals: {celsius: @celsius, threshold_timestamp: @threshold_timestamp, timestamp: @timestamp, property: @property }%>
        <div class="d-flex justify-content-between">
            <%= link_to "Menu Propriedades", properties_path, class: "btn blt-md btn-outline-secondary" %>
            <%= link_to "Painel Hídrico", properties_path, class: "btn btn-md btn-outline-secondary" %>
            <%= link_to property_path(@property), method: :delete, data: {confirm: "Tem certeza que deseja deletar esta propriedade?"}, class: "btn btn-md btn-outline-danger" do %>
                <div class="text-center">
                    <i class="fas fa-trash"></i><small class="ml-2">Excluir Propriedade</small>
                </div>
          <% end %>
        </div>
    </div>
  <div class="map mt-3">
    <img src="<%= @map %>">
  </div>
</div>


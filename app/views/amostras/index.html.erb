<div class="d-flex">
  <div>
	  <%= render "shared/sidebar" %>
  </div>

  <div class="w-80" style="width: 86vw;"> 
    <!-- ModaL div for render new partial-->
    <div id="modal-window" class="modal hide fade" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-dialog modal-lg modal-xlg" role="document">
        <div class="modal-content"></div>
      </div>
    </div>

    <!-- Content Wrapper -->
    <div id="content-wrapper" class="d-flex flex-column mt-4 col-lg-12">
	    <section class="jumbotron text-center ml-2">
        <div class="container">
          <div>
            <h1 class="jumbotron-heading"><strong>Amostras de Solo</strong></h1>
            <p class="lead text-muted">
              Nesta página, você tem acesso as suas amostras de solo que foram coletadas e cadastradas.
            </p>
            
          </div>
        </div>
      </section>

      <div class="ml-2">
        <div class="d-flex flex-row align-items-center justify-content-between ml-2">
        <%= link_to ' Criar nova amostra de solo', new_amostra_path, {:remote => true, 'data-toggle' =>  "modal", 'data-target' => '#modal-window', :class => "btn btn-lg btn-outline-primary favorite-btn" }  %>
        <div class="align-self-flex-end mr-2">
          <% if @amostras.present? %>
            <span><strong><%= @amostras.count %> Amostras cadastras no total ✾</strong></span>
          <% else %>
            <h5 style="margin: 0;">Nenhuma amostra cadastrada ainda.</h5>
          <% end %>
        </div>
      </div>

      <hr/>
      
      <div class="ml-2 mt-4">
        <% if @amostras.present? %>
          <%= render  "amostras" %>
        <% else %>
          <h5 class="ml-2">👆 Adicione novas amostras de solo.</h5>
          <% end %>
      </div>
    </div>
  
    
  </div>
</div>

 